#!/usr/bin/env bash
INSTALL_PATH="$1"

# Set up the PATH environment variable to include the Flutter binary
echo "PATH=$INSTALL_PATH/bin:$PATH"

# Tell Flutter we're using a custom repo
echo "FLUTTER_GIT_URL=https://github.com/superlistapp/flutter.git"
