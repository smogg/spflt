#!/usr/bin/env bash
set -eo pipefail # remove the 'u' for now

version="${1:-}"
install_path="${2:-}"

if [[ -z "$version" || -z "$install_path" ]]; then
  echo "Missing version or install_path"
  exit 1
fi
